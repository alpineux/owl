import React from "react";
import Fade from "~/components/Navbar/Fade"
import FooterDefault from "~/components/Footer"

export const Navbar = (props) => (
	<Fade />
);

export const Footer = (props) => (
	<FooterDefault />
);


